#!/bin/bash
# encoding: utf-8
# Name  : MyCreateAPIModuleAll.sh
# Descp : used for 
# Author: jaycee
# Date  : 29/09/17 11:44:50 +0800
__version__=0.1

set -x                     #print every excution log
set -e                     #exit when error hanppens

DILPATH=`pwd`/idl
FILEPATTEN=${IDLPATH}/*.fidl

if [ -d ${IDLPATH} ]
then
    find ${IDLPATH} -type f -name "*.fidl" -exec CreateAPIModule {} \;
fi



